
const LearningEntry = () => {
    return(
        <>

        </>
    );
};

export default LearningEntry;
// This is a main screen that contains *two* options written in large cards.
// The cards say "English" and "Mathematics", and each one navigates the learner to a different front screen
// This view has a HeaderBar.