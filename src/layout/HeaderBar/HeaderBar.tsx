
const HeaderBar = () => {

    return (
        <>
        </>
    );
};

export default HeaderBar;

// The HeaderBar contains different controls depending on what screen the learner is on.
// If the learner has navigated to LearningEntry, the HeaderBar contains a profile icon which, when clicked,
// take them to the EditingProfile screen.