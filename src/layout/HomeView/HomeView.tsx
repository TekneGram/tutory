
const HomeView = () => {

    return(
        <>

        </>
    );
};

export default HomeView;
// This is a welcome screen.
// It displays the following
// A card to "create a new learner profile"
// LearnerCard
// When the learner clicks on "create a new learner profile", they navigate to the NewProfile screen.
// When the learner clicks on the LearnerCard's Enter button, they are navigated to the LearningEntry screen