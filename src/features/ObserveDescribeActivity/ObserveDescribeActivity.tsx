import type { ActivityComponentProps } from "@/features/ActivityDisplay/types/activityDisplay.types";

const ObserveDescribeActivity = (_props: ActivityComponentProps) => {

    return (
        <>
        </>
    );
};

export default ObserveDescribeActivity;
