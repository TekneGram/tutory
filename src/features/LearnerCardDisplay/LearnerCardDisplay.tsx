import LearnerCard from "./LearnerCard";

const LearnerCardDisplay = () => {
    // Follow rules to use a hook and service to ports/adapters get data from learners table in backend.
    // Hydrate this, mapping each row from learners table to LearnerCard
    return (
        <>
        </>
    );
};

export default LearnerCardDisplay;
// Display multiple LearnerCards depending on how many users there are in the app.