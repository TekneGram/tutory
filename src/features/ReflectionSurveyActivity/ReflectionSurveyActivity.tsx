import type { ActivityComponentProps } from "@/features/ActivityDisplay/types/activityDisplay.types";

const ReflectionSurveyActivity = (_props: ActivityComponentProps) => {

    return (
        <>
        </>
    );
};

export default ReflectionSurveyActivity;
